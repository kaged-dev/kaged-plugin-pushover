{
	"name": "@kaged/plugin-pushover",
	"description": "Hosted tier-3 notification channel for kaged: posts session notifications to the Pushover service",
	"version": "0.1.0",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kaged-dev/kaged-plugin-pushover.git"
	},
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"files": [
		"src"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@kaged/plugin-types": "^0.0.2",
		"@types/bun": "^1.3.0",
		"typescript": "^5.8.0"
	}
}
